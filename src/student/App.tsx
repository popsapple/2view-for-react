import './app.scss'
import type SocketFactory from '@src/util/socket';

function App(props: {socket: SocketFactory | null}) {
  return (
      <div>
      dfsfsdf
    </div>
  )
}

export default App
